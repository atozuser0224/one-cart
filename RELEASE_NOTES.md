# ONE CART v0.2.1-photon-dev · Windows x64

온라인 방이 약 35초 뒤 종료되던 문제와 전체화면 조작, 첫 실행 화면을 수정한 사전 릴리즈입니다. 친구와 함께 **모두 이 버전으로 업데이트**해 주세요.

## 수정 내용

- 연결에 성공한 뒤에도 남아 있던 35초 접속 타이머를 해제합니다. 이전 버전에서는 이 타이머가 정상적인 방까지 종료시켰습니다.
- 방 없음·방 닫힘·인원 초과·접속 제한·시간 초과를 구분해 한국어로 안내합니다.
- `Alt+Enter` 또는 `F11`로 전체화면과 창 모드를 전환합니다. 전체화면은 데스크톱 해상도의 테두리 없는 모드이며, 창 모드로 돌아오면 이전 크기를 복원합니다.
- 새 사용자는 전체화면으로 시작합니다. 기존 사용자의 저장된 화면 설정은 유지합니다.
- “처음 오셨군요” 화면의 **온라인 방으로 계속** 버튼을 항상 보이는 하단에 고정하고, 스크롤바를 추가했습니다.
- 아직 상품이 배정되지 않은 진열대의 재입고 예외와 참가자 PC에서 불필요하게 생성되던 엄마의 길찾기 오류를 수정했습니다.

## 실행

1. Assets의 `ONE-CART-v0.2.1-photon-dev-Windows-x64.zip`을 다운로드합니다. `Source code`는 실행용 게임이 아닙니다.
2. 기존 게임을 종료하고 ZIP 전체를 새 폴더에 풉니다.
3. `OneCart.exe`를 실행합니다. 전체화면 전환은 `Alt+Enter` 또는 `F11`입니다.
4. 한 명이 방을 만들고, 친구가 같은 버전에서 8자리 방 코드로 참가합니다. 최소 2명이 준비하면 호스트가 시작합니다.

Windows x64와 인터넷 연결이 필요합니다. 플레이어의 Photon 계정·서비스 키·Steam 설치는 필요하지 않습니다. 기존 설정과 저장은 Windows 사용자 데이터 폴더에 유지됩니다.

## 검증 범위

Unity 6000.5.5f1 비개발 Windows 빌드가 오류·경고 0으로 완료됐습니다. 첫 화면 버튼·전체화면 전환, 같은 PC의 두 프로세스를 이용한 Photon Cloud 로비 50초 대기·방 오류 후 재시도·게임 진입 후 45초 연결 유지 및 상태 동기화를 통과했습니다. 양쪽의 예기치 않은 런타임 오류는 0건입니다. 의도적으로 요청한 없는 방에 대한 SDK 오류 1건은 예상 결과로 별도 확인했습니다. 상세 결과는 저장소의 `VALIDATION.json`에 기록했습니다.

서로 다른 PC·4인·실제 마이크·전체 경제/구조/스테이지 진행은 아직 미검증입니다. 완성판이 아닌 개발 중 사전 릴리즈입니다. SHA-256 파일과 제3자 라이선스를 함께 제공합니다.
